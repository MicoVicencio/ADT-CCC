<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.CaintaCatholicCollege">

        <activity
            android:name=".SplashActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".admission"/>
        <activity android:name=".academics"/>
        <activity android:name=".about"/>
        <activity android:name=".contact"/>
        <activity android:name=".master"/>
        <activity android:name=".history"/>
        <activity android:name=".facilities"/>
        <activity android:name=".missionVision"/>
        <activity android:name=".recognition"/>
        <activity android:name=".MainActivity"/>
    </application>

</manifest>
